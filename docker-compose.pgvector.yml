services:
  pgvector:
    container_name: pgvector-standalone
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: testvdb
    volumes:
      - ./volumes/pgvector:/var/lib/postgresql/data
    ports:
      - "5432:5432"
